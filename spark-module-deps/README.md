Module: Collection of the Apache Spark dependencies
-----------------------------

The main task
    $ ./gradlew :spark-module-deps:installDist
    
It generates `build/install/lib` folder containing all the dependencies needed to run/execute Apache Spark jobs.